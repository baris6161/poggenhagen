# Kopie als .env.local anlegen (wird nicht ins Git aufgenommen).
# AI_GATEWAY_API_KEY=
# NEXT_PUBLIC_SITE_URL=https://deine-produktions-domain.de
# VERCEL_TOKEN=   # nur für: vercel deploy --prod --token $VERCEL_TOKEN
